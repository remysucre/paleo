#lang racket

(provide (all-defined-out))

(define (conj x y) '()) ; TODO

(define (SMTSolve f) '()) ; TODO

(define (subst x y z) '())
(define (substs xs ys z) '()) ; TODO

(define (Node f) '()) ; TODO decode MUC for checkconflict

(define (Rename p) '()) ; TODO